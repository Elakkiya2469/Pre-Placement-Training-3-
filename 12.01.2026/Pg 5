class LongestWord {
    public static void main(String[] args) {
        String s = "Zoho Corporation is hiring Java developers";
        String[] words = s.split(" ");

        String longest = "";
        for (String w : words) {
            if (w.length() > longest.length())
                longest = w;
        }

        System.out.println(longest);
    }
